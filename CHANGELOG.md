# تغییرات

منبع حقیقت محصول شاخهٔ `main` است. این پرونده جمع‌بندی نسخهٔ مرجع است تا صدها کامیت و برنچ پراکنده جایگزین «چه چیزی الان در محصول است» نشوند.

## 1.0.0 — 2026-09-13

نسخهٔ نهایی تات کیدز بر اساس `main` در کامیت `6119515e` (ادغام [#236](https://github.com/mahmoudhamzeh/roshdyar2/pull/236)).

### هویت و ورود
- برند تات کیدز (TatKids) در رابط، لوگو و فاوآیکون
- ثبت‌نام و ورود با OTP پیامک (Idekavan / sms.ir)
- بازیابی رمز با OTP و مخفی‌کردن ورود ادمین از مسیر عمومی
- JWT، محدودیت نرخ درخواست و آدرس پایه API برای production

### رشد و سلامت کودک
- ویزارد افزودن/ویرایش کودک و مدیریت فرزندان
- نمودار رشد WHO (قد، وزن، دور سر) با تاریخ شمسی
- صفحه «رشد کودک من» با راهنمای سنی و دستیار
- پرونده سلامت، تحلیل سلامت، چکاپ و آزمایش
- کارت واکسیناسیون، وضعیت واکسن، خروجی PDF و یادآورها

### مجله سلامت
- مقاله، ویدیو و پادکست با دسته‌بندی، برچسب، نویسنده و سئو
- پنل مدیریت محتوا و رفع سازگاری Postgres در مجله

### فروشگاه و فروشنده
- فروشگاه، دسته‌بندی، مهارت/هدف رشدی، صفحه محصول
- امتیاز، فیلتر، نان‌ریزه، نظرات نیازمند تأیید، کالای مشابه
- سبد خرید، سفارش، پنل فروشنده (حقیقی/حقوقی، مدارک، مالی)
- بررسی فروشنده در ادمین و سایدبار گروه‌بندی کالا

### پیام، یادآور و موبایل
- صندوق پیام کاربر، پیام گروهی ادمین، تیکت پشتیبانی
- داشبورد موبایل: کاشی خدمات، اسکرول مقالات/ویدیو، کشوی ناوبری
- سیستم رنگ فیروزه‌ای و تایپ فارسی یکدست

### زیرساخت
- پایگاه پیش‌فرض SQLite (WAL)؛ مسیر PostgreSQL برای ترافیک بالاتر
- اسکریپت استقرار، بکاپ و مهاجرت (`scripts/`)
- کاتالوگ زنده API روی `GET /api`

### خارج از این نسخه
پول‌ریکویست‌های باز و برنچ‌های `cursor/*` که در `main` ادغام نشده‌اند جزء ۱.۰ نیستند. MongoDB هم مسیر آزمایشی است؛ محصول روی SQLite / Postgres است.
